import { Heading } from '@chakra-ui/react';

export const HomePage = () => {
  return (
    <Heading>Create an account or log in to access your contact list</Heading>
  );
};
export default HomePage;
